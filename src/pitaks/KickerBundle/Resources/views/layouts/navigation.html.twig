<nav role="navigation" class="navbar navbar-default navbar-inverse">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a href="#" class="navbar-brand">Pitaks</a>
    </div>
    <!-- Collection of nav links and other content for toggling -->
    <div id="navbarCollapse" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
            <li ><a href="{{ path('pitaks_kicker_homepage') }}">Home</a></li>
            {% if app.user %}
                <li><a href="{{ path('fos_user_profile_show') }}">Profile</a></li>
            {% endif %}
            <li><a href="{{ path('show_users') }}">Players</a></li>
            {% if is_granted("ROLE_ADMIN") %}
                <li><a href="{{ path('adminView') }}">Admin</a></li>
            {% endif %}
        </ul>
        <ul class="nav navbar-nav navbar-right">
            {% if app.user %}
                <li><a href="{{ path('fos_user_security_logout') }}">Exit</a></li>
            {% else %}
                <li><a href="{{ path('fos_user_security_login') }}">Login</a></li>

            {% endif %}
        </ul>
    </div>
</nav>
